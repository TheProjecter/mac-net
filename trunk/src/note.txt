2009-1117
更改REQ_WATIT_TIME为20毫秒，原先500太大影响性能







不正常现像：有时单片机的串口会发出乱乱，在之前版本未出现过。 此时需关机一段时间，复位也没用
2009-1115
加入clear statistic功能
可更改波特率功能

2009-1110
更改了trans.c里解析到发送数据时的处理，取消了临时数组
有个BUG， hal_radio當收到包時如果CRC錯誤，沒有删除掉这个包
加入参数统计功能, ok
加入对收到重复包的处理（如果长度一样，MAC序号和MAC源地址一样）则直接删除


2009-1109
加入了RSSI, TEMPERATURE参数的读取,OK
2009-1108
修改UART BUFFER长度为255
修改串口中断程序，考虑串口缓冲区满的情况
tran.c里对长度超过FRAME_MAX_LEN的情况做处理


在填入路由时需要先查找一下自己是不是已经在里面了，避免环路出现

todo but no time:
*在检查路由时先检查是否失效将更有效率，并且在路由管理初始化时标识全部失效。因为现在rt_cache里不需要移出队列，路由的失效仅在位图里管理。使用一个变量来指示下次插入的路由的位置

*发送数据用的queue里可以改进。 现在是查看一个包，如果没有这个包的目标地址的路由则先发路由请求，等待一段时间后重新处理。其实发完这个包的请求后可以去处理下一个包了。

*现在mac_do_send，只接受一段数据。 可以改成接受两段数据。这样nwk_send_queue里就不用为每个要发的数据包分配网络头了，节省了存储空间

*使用个cache缓存最近接收到的多个(req_id, srcnode),(bcast_id, srcnode)，而不是用简单的一个变量进行处理。可以用hash来组织。对于广播很频繁的场景，这个挺有用

*mac 缓冲区可能有BUG， 可能跟中断服务程序不一致

*uart 中断里对指针的移动可能也有问题，队列满时的处理，填进去还是丢弃？

*统计信息可能有问题，有遗漏


2009-1106

已经可以用了， 在中间节点转发时先延时一段随机时间，否则会有些不想要的结果（如目标收到多个包，源节点以为发送失败）


